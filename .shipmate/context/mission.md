# Mission

## Purpose

`ui-plugin-sdk` is a UI plugin SDK intended to provide a standardized interface for building and integrating UI plugins. The project is owned by the **frontend** team.

## Status

**Greenfield** — the repository has been initialized but contains no source code yet. The first commit established the repo scaffold and Shipmate tooling configuration.

## Goals

> _Not yet documented._ Define the SDK's target consumers, the plugin contract it exposes, and the problems it solves once initial design decisions are made.

## Non-Goals

> _Not yet documented._ Clarify what this SDK deliberately does not cover (e.g., backend plugin systems, server-side rendering orchestration) once scope is established.
