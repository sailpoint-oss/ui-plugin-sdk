# Domain

## Key Concepts

> _Not yet established in code._ The following are anticipated domain terms based on the project name and standards:

| Term | Expected Meaning |
|------|-----------------|
| Plugin | A self-contained UI extension that integrates into a host application |
| SDK | The library providing the contract, utilities, and types for plugin development |
| Host | The application that loads and renders plugins |
| Plugin contract | The interface or API surface a plugin must implement |

## Business Rules

> _Not yet documented._ Define plugin lifecycle rules, registration mechanics, and isolation boundaries once design work begins.
