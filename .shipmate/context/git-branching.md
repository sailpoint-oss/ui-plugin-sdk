# Git Branching

## Current Branches

- `main` — the sole branch; serves as the default and primary branch.

## Branching Strategy

> _Not yet established._ With only one commit and one branch, no branching convention has been demonstrated. Define a strategy (e.g., trunk-based, Gitflow, GitHub Flow) as the team begins active development.

## Commit Message Style

The single existing commit uses a lowercase, descriptive style:

```
first commit
```

> _Insufficient history to infer a convention._ Establish a commit message format (e.g., Conventional Commits) early to maintain a clean history.
