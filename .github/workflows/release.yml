name: Release

# PHASE 1 (current): publishes on GitHub Release, token auth.
# `workflow_dispatch` is retained for manual/dry-run testing.
#
# PHASE 2 (after the first real publish succeeds): swap token auth for OIDC
# trusted publishing. See the checklist at the bottom of this file.
on:
  release:
    # `published` covers both brand-new releases and drafts being published.
    # A release left as a draft does not trigger anything.
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish, without a leading v (e.g. 0.0.1)'
        required: true
        type: string
      dry_run:
        description: 'Pack and validate without publishing'
        required: false
        type: boolean
        default: true
      create_release:
        description: 'Also tag the commit and create a GitHub Release'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: Publish to npm
    runs-on: ubuntu-latest

    permissions:
      contents: write # tag the commit + create the GitHub Release (dispatch only)

    steps:
      # On a release event GITHUB_SHA is the commit the release targets,
      # so the default checkout is already the right tree.
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      # registry-url writes an .npmrc with `_authToken=${NODE_AUTH_TOKEN}`,
      # which pnpm expands from the env on the publish steps below.
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Resolve version
        id: meta
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_PRERELEASE: ${{ github.event.release.prerelease }}
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "release" ]]; then
            RAW="$RELEASE_TAG"
          else
            RAW="$INPUT_VERSION"
          fi

          VERSION="${RAW#v}"

          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::'$RAW' is not a valid semver version"
            exit 1
          fi

          # Never let a prerelease take over the `latest` dist-tag. A release
          # flagged pre-release in the GitHub UI is honoured too, even if its
          # version string looks stable.
          if [[ "$VERSION" == *-* || "$RELEASE_PRERELEASE" == "true" ]]; then
            NPM_TAG=next
            PRERELEASE=true
          else
            NPM_TAG=latest
            PRERELEASE=false
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "npm_tag=$NPM_TAG" >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"

      - name: Summarize
        run: |
          {
            echo "### Release ${{ steps.meta.outputs.version }}"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| trigger | \`${{ github.event_name }}\` |"
            echo "| dist-tag | \`${{ steps.meta.outputs.npm_tag }}\` |"
            echo "| prerelease | \`${{ steps.meta.outputs.prerelease }}\` |"
            echo "| dry run | \`${{ inputs.dry_run == true }}\` |"
            echo "| commit | \`${{ github.sha }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Type check
        run: pnpm run typecheck

      - name: Test
        run: pnpm test

      # package.json is committed as 0.0.0; the release tag (or input) is the
      # source of truth for the published version.
      - name: Set package version
        run: pnpm pkg set version="${{ steps.meta.outputs.version }}"

      # `files: ["dist"]` and there is no prepack hook, so this must be explicit
      # or the tarball ships no code.
      - name: Build
        run: pnpm run build

      - name: Verify tarball contents
        run: |
          set -euo pipefail
          pnpm pack --pack-destination "$RUNNER_TEMP"
          echo "--- tarball contents ---"
          tar -tzf "$RUNNER_TEMP"/*.tgz

      # --no-git-checks is required: `pnpm pkg set version` dirties the tree,
      # which pnpm otherwise refuses to publish from.
      - name: Publish to npm (dry run)
        if: ${{ inputs.dry_run == true }}
        run: pnpm publish --dry-run --no-git-checks --tag "${{ steps.meta.outputs.npm_tag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish to npm
        if: ${{ inputs.dry_run != true }}
        run: pnpm publish --no-git-checks --tag "${{ steps.meta.outputs.npm_tag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Only for manual runs — on a release event the release already exists.
      # Releases created here use GITHUB_TOKEN, which by design does not
      # re-trigger this workflow, so there is no publish loop.
      - name: Create GitHub Release
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run != true && inputs.create_release }}
        run: |
          set -euo pipefail
          gh release create "v${{ steps.meta.outputs.version }}" \
            --target "$GITHUB_SHA" \
            --title "v${{ steps.meta.outputs.version }}" \
            --generate-notes \
            ${{ steps.meta.outputs.prerelease == 'true' && '--prerelease' || '--latest' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
# ---------------------------------------------------------------------------
# PHASE 2 — switching from NPM_TOKEN to OIDC trusted publishing
#
# Do this only after a real (non-dry-run) publish has landed on npm, since a
# trusted publisher cannot be configured for a package that does not exist yet.
#
# 1. On npmjs.com, go to
#      https://www.npmjs.com/package/@sailpoint/ui-plugin-sdk/access
#    and add a GitHub Actions trusted publisher:
#      organization: sailpoint-oss
#      repository:   ui-plugin-sdk
#      workflow:     release.yml     <-- must match this filename exactly
#      environment:  (leave blank)
# 2. Bump `packageManager` in package.json to pnpm >= 10.18 — earlier 10.x
#    passes unresolved ${VAR} placeholders from .npmrc straight through, which
#    makes OIDC publishes fail with a 404. Avoid pnpm 11.0.8 (OIDC regression).
# 3. Edit this workflow:
#      - add `id-token: write` to the job's `permissions:`
#      - delete `registry-url:` from the setup-node step (the .npmrc it writes
#        is what breaks OIDC; trusted publishing needs no credentials)
#      - drop both `NODE_AUTH_TOKEN` env blocks
#      - add `env: { NPM_CONFIG_PROVENANCE: true }` to the publish step
# 4. Delete the NPM_TOKEN repository secret.
# ---------------------------------------------------------------------------
